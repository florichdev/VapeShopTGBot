# 🚀 Инструкция по настройке Vape Shop

## Быстрый старт

### 1. Создайте конфигурационный файл

```bash
# Скопируйте пример конфигурации
copy config.example.py config.py
```

### 2. Заполните config.py своими данными

Откройте `config.py` и замените:

```python
# Telegram Bot
BOT_TOKEN = 'ваш_токен_от_@BotFather'

# Администраторы (Telegram ID)
ADMIN_IDS = [ваш_telegram_id]

# Flask
SECRET_KEY = 'случайная_строка_для_сессий'

# Уведомления
NOTIFICATION_CHAT_ID = ваш_telegram_id
```

### 3. Как получить Telegram ID?

Напишите боту [@userinfobot](https://t.me/userinfobot) и он покажет ваш ID.

### 4. Как создать Telegram бота?

1. Напишите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `config.py`

### 5. Генерация SECRET_KEY для Flask

```python
import secrets
print(secrets.token_hex(32))
```

Скопируйте результат в `SECRET_KEY`.

### 6. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 7. Запуск

```bash
python run.py
```

Это запустит:
- Telegram бота
- Админ-панель на http://localhost:5000

## Настройка магазина

### Первый вход в админ-панель

1. Откройте http://localhost:5000
2. Создайте аккаунт администратора
3. Войдите в систему

### Добавление товаров

1. Перейдите в раздел "Товары"
2. Нажмите "Добавить товар"
3. Заполните информацию:
   - Название
   - Описание
   - Цена
   - Категория
   - Изображение (опционально)

### Категории

Создайте категории товаров для удобной навигации в боте.

## Структура базы данных

База данных создастся автоматически при первом запуске в файле `shared_database.db`.

Основные таблицы:
- `users` - пользователи бота
- `products` - товары
- `categories` - категории
- `orders` - заказы
- `cart` - корзины

## Настройка платежей (опционально)

Если хотите принимать платежи, настройте платежного провайдера в `config.py`:

```python
PAYMENT_TOKEN = 'токен_от_провайдера'
PAYMENT_PROVIDER = 'yookassa'  # или другой
```